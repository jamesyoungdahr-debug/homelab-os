# Auto-generates authentik.env/dashboard.env with random secrets so the
# stack works with no manual setup. Must run after `users` (needs the
# target's /etc/hostname). dontChroot: false so it writes into the TARGET.
---
dontChroot: false
timeout: 30
script:
    - command: "bash /usr/local/bin/homelab-generate-secrets.sh"
