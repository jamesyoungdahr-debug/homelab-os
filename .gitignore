# BlueBuild / image build output
distro/output/
distro/*.iso
distro/*.qcow2

# Moved to its own repo: github.com/jamesyoungdahr-debug/HoltOS. Still lives
# on disk locally for building/testing, but no longer tracked here.
distro-arch/

# Never commit filled-in secrets — only the .example templates
distro/secrets/
**/*.env
!**/*.env.example

# Session-local Claude Code harness state, not project source
.claude/*.lock
.claude/settings.local.json
